on:
  workflow_dispatch:

jobs:

  call-workflow-passing-data:
    uses: ananta-verse/re-usable-workflow/.github/workflows/my-action.yml@main
    with:
      word-to-print: IM INPUT PARAMETER
    secrets: inherit
    # secrets:
    #   envPat: ${{ secrets.envPAT }}
